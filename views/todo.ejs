<html>
  <head>
    <title>TodoList</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>TodoÉäÉXÉg</h1>
  </body>
  <div class="wrapper">
    <ul class="task-list">
      <% taskList.forEach(function(taskItem) { %>
          <li class="main-list__task">
              <div class="task">
                  <p class="task__title"><%= taskItem.title %></p>
                  <p class="task__content"><%= taskItem.content %></p>
                  <form action="/todo" method="post">
                    <input type="hidden" name="id" value="<%= taskItem.id %>">
                    <button type="submit" name="button">Ç±ÇÃToDoÇçÌèú</button>
                  </form>
              </div>
          </li>
      <% }); %>
  </ul>
</div>
</html>